Seis: OOTAB MERGE'I

# Kirjutaja: kuulamisabi võrdlusklipid ja Kore välteviga (haru `kuulamisabi-haal`)

Võrdluslink: https://github.com/silverjaanus/harjutaja/compare/main...kuulamisabi-haal

Kuulamisabi ise (nupp „Kuula kolme pikkust") on juba mainis, taust failis `handover/arhiiv/2026-09-11_16.md`. See haru lisab Charoni häälega võrdlusklipid (`kirjutaja/audio2/`), parandab Kore klipid `rata` ja `tupa` ning toob kaasa välteskanni ja kuulamislehed.

Märkus: see fail tehti merge'i konflikti lahendamisel 16. sept. Haru oli tehtud vanalt mainilt ja kirjutas pooleli töö `Handover.md`-sse; uue korra järgi (`handover/README.md`) on see nüüd siin ja `Handover.md` jäi mainist muutmata.

- **Võrdlusklipid on valmis ja commitis: 920 klippi 924-st** (16. sept, haru `kuulamisabi-haal`, commit `baf7c23`; enne seda 894 / 924, commit `ef0ae37`). Skript `tools/run_audio2.ps1` (kutsub `audio_gen.py --voice Charon --out audio2 --raw audio2_raw`) jätab olemasolevad vahele ja jätkab.
  - **Päeva kulg.** Hommikune ring (10.31–10.52) tegi 368 klippi 37 päringuga ja jäi tasuta taseme päevalimiidi taha. Silver lisas raha ning pärastlõunane ring (12.24–12.50) tegi **469 klippi juurde**. Lõpuks tuli uuesti `PEATUS: Gemini päevalimiit täis` — HTTP 429, mille kehas on „PerDay". Eelmise ringi logi on failis `tools/audio2_run_osa1.txt`.
  - **Puudu on 30 sõna:** hiki, jõgi, jõkki, kadakat, kadeda, kapet, katte, ludi, lõpusat, makkusat, naba, nubukat, nägus, nõgese, nõgest, nõkest, otava, pakkane, pegi, piduri, rattast, ritta, rõttu, rõttud, rõtu, sattam, sõta, sõtta, süda, tugi. Osa neist on Charoni järjekindlad valesti lugemised (kontroll kuulis „makkusat" asemel „mukusat", „pegi" asemel „Teki"), nagu põhihäälega jäid tegemata `hope` ja `rotu`. Nende puhul kukub äpp tagasi küsimuse häälele — see on ette nähtud käitumine, mitte viga.
  - **Tähelepanek arve kohta:** raha lisamisest hoolimata vastas API endiselt päevakvoodiga (`PerDay`). Tasub kontrollida, kas API-võti käib sama Google Cloudi projekti alla, millele arveldus lisati, ja kas selle projekti tasuline tase on Gemini TTS-i jaoks sisse lülitatud. Ülejäänud 30 klippi saab niikuinii homme järele teha.
  - **16. sept: päevakvoot oli lähtestunud ja järelering läks läbi.** `run_audio2.ps1 -MaxRequests 300 -Batch 12` tegi 30-st **26 klippi** ära ainult 7 TTS-päringuga, päevalimiiti ei tulnud. **Puudu jäi 4: `jõkki`, `rõttud`, `rõtu`, `sõta`** — kõik neli kukuvad läbi transkriptsioonikontrolli, sest Charon loeb need järjekindlalt valesti (kuuldi „Jükki", „Ruttud", „Ruttu", „Sutta"). Need neli kukuvad äpis tagasi küsimuse häälele, nagu ette nähtud. `audio2` = **920 / 924**.
- **Kuulamisleht (commitid `7a3d9f1`, `ef0ae37`):** `tools/audio2_vordlus.html` mängib 18 kolmikut kõrvuti mõlema häälega, iga kolmiku juures kaks nupurida (Küsimus kaustast `kirjutaja/audio`, Võrdlus kaustast `kirjutaja/audio2`), sildid lühike / pikk / ülipikk ja nupp „kõik kolm". Serveerida repo juurest `python -m http.server 8901`, aadress http://localhost:8901/tools/audio2_vordlus.html. **16 kolmikut on mõlema häälega täielikud**; kade–kate–katte ja sadam–satam–sattam on poolikud ja seisavad nimekirja lõpus. Võrdluslink: https://github.com/silverjaanus/harjutaja/compare/main...kuulamisabi-haal
- **KUULAMISKONTROLL ON TEHTUD (15. sept) — CHARON LÄBIS.** Silver kuulas kahes ringis läbi ~30 kolmikut mõlema häälega. Charonil ei leidnud ta ühtegi väldet valesti. Otsus: **võrdlus jääb Charoni häälega**, `audio2` jääb alles.
- **AGA: kaks viga leiti KORE häälest, mis on juba mängus sees.** `kirjutaja/audio/rata.mp3` kõlab „raata" ja `kirjutaja/audio/tupa.mp3` kõlab „tuupa" — pikas vormis on venitatud täishäälik, mitte sulghäälik. Charoni samad klipid on korras. Vead tulid välja alles siis, kui selgus, et esimeses ringis kuulas Silver ainult ülemist (Kore) rida.
  - **Mõju on piiratud.** `rata` ja `tupa` **ei ole küsimussõnad** — `kirjutaja/data.js`-i 376 kirje hulgas on `rada` ja `tuba`, mitte nende pikad vormid. Neid kuuleb laps ainult vea-järgsel kaardil (`playAll`, kaust `audio/`). Uus kuulamisabi mängib võrdluse `audio2`-st, kus need klipid on korras.
  - **TEHTUD 16. sept (commit `baf7c23`).** Vanad `audio/rata.mp3` ja `audio/tupa.mp3` kustutati ja `audio_gen.py --voice Kore --out audio --raw audio_raw --max-requests 20 --batch 4` tegi need ühe TTS-päringuga uuesti. Sama ringiga tuli ära ka üks ammu puudu olnud Kore klipp (`audio` = 929 → 930).
  - **Mõõdetud üle ffmpeg-iga** (`silencedetect=noise=-40dB:d=0.03`), täishääliku pikkus enne sulghäälikut, võrdluses sama hääle I-välte vorm. **`rata` 0,267 s vs `rada` 0,298 s (suhe 0,90)** ja **`tupa` 0,135 s vs `tuba` 0,125 s (suhe 1,09)** — mõlemad tublisti alla 1,3 lävendi, seega venitatud täishäälikut enam ei ole. Sulghääliku sulg on seevastu pikas vormis õigesti pikem: `rata` 0,149 s vs `rada` 0,038 s (3,9×), `tupa` 0,141 s vs `tuba` 0,083 s (1,7×). Silveril tasub siiski korra kõrvaga üle kuulata.
  - **Süstemaatiline ülekuulamine: Silver ütles 16. sept jah.** Et ta ei peaks 929 klippi läbi kuulama, tegin enne masinliku sõelumise (commit `ede0919`).
    - **`tools/audio_valde_skann.py`** mõõdab ffmpeg-iga (`silencedetect`) iga klipi täishääliku pikkuse enne sulghäälikut ja võrdleb kolmiku sees pikka ja ülipikka vormi lühikesega. **Ainult täishääliku suhe loeb;** sulu suhe visati välja, sest lühikese vormi g/b/d ei ole vaikus ja see suhe andis ainult valepositiive (129 → 15). Lisaks võrdleb sama sõna Charoni klipiga: kui Kore venitab ja Charon mitte, on kahtlus tugevam. Lävend `V_MAX = 1,3` (vana vigane `rata` oli 1,46×).
    - **Tulemus: 15 kahtlast klippi 882-st.** Järjestatud, kõige kahtlasem ees: `tuka` 1,73×, `rattu` 1,72×, `vikkurit` 1,60×, `jaki` 1,58×, `nuka` 1,57×, `make` 1,55×, `patta` 1,55×, `happut` 1,48×, `nukka` 1,47×, `sipelga` 1,39×, `luke` 1,38×, `jutukas` 1,35×, `putuka` 1,34×, `ruki` 1,34×, `puttuka` 1,33×. Neist 14 on Charonil korras — ainult `putuka` on ka Charonil pikk, seega tõenäoliselt valepositiiv. Nimekiri ka failides `tools/audio_valde_skann.txt` ja `.json`.
    - **`tools/valde_kontroll.html`** on kuulamisleht nendele 15-le: iga rea peal Kore kolmik ja Charoni kolmik, nupp „kõik kolm", märkeruudud korras / vigane ja allservas kopeeritav nimekiri vigastest. **Juhis on lehe sees** (eelmise korra õppetund). Serveerida repo juurest `python -m http.server 8902`, aadress http://localhost:8902/tools/valde_kontroll.html. Kontrollitud brauseripaanis, leht laeb ja kõik 90 klippi on olemas.
    - **Ootab Silverit:** kuula need 15 üle ja saada vigaste nimekiri — siis saab need Kore häälega uuesti genereerida, nagu `rata` ja `tupa`.
